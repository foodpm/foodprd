---
name: foodprd
description: 依据原型截图/设计稿图片，产出符合固定编号规范与既定写作风格的中文 PRD，支持两类场景：(1) 客户端/C端/H5/小程序/App 页面；(2) 管理后台/运营后台 CRUD 模块。当用户发来原型图、设计稿、UI 截图、Axure/Figma 截图并要求"写 PRD、写需求文档、写产品文档、按之前的风格写需求"时使用。收到截图后先判断是前端还是管理后台，再套用对应风格样例与编号规范。
agent_created: true
---

# 原型截图转 PRD

把一张或多张原型截图，转写成可直接交付研发的中文 PRD。**先判定是客户端还是管理后台，再套用对应风格与编号。**

## 使用流程

### 第 0 步：模块影响分析（动笔前必做）

写任一功能前，先判断它对现有系统的影响，落到 PRD 里：

- **逻辑影响**：是否改变既有状态机、统计口径、权限判断、数据刷新链路；是否与其他模块共享同一份数据/同一状态。
- **展示影响**：是否复用既有组件、是否改变全局一致性既有规则（占位图、文案、空/异常态）。

把受影响模块的"旧数据处理"和"跨端/跨角色数据同步"写进对应模块，不要只写新功能本身。

### 第 0.5 步：判定客户端还是管理后台

收到截图后，**先判定类型**，决定读取哪套样例与规范：

- **管理后台特征**：顶部有筛选/查询条件区、字段密集的列表、状态机（进行中/已下架等）、操作日志、分域/数据权限、二次确认弹窗、批量操作、导出按钮、erp/本系统取数分支。典型原型是运营后台的列表页、表单弹窗、详情页。
- **客户端特征**：底部悬浮 bar、活动头图/轮播、分享、三态倒计时（未开始/进行中/已结束）、tab 切换、空状态插画、拉票/投票类 C 端交互。典型原型是 H5/小程序/App 的活动页、商详页。

判定后：

- **管理后台** → 读 `references/sample-prd-admin.md`（权威风格样例）+ `references/admin-conventions.md`（模块结构 + 后台专属规则），按后台模块顺序（查询 → 列表字段 → 创建 → 详情 → 编辑 → 删除 → 设置 → 操作日志 → 分域）与后台编号（一、/ 1、/ a. b. c. 或（1）（2））写作。
- **客户端** → 读 `references/sample-prd.md`（权威风格样例）+ `references/writing-rules.md`（元素→必写规则）+ `references/checklist.md`（产品注意事项清单），按既有客户端规范写作。

两类最终都读 `references/checklist.md` 做通用产品设计自检（同一原型可能同时含前后端，分别套用）。

### 第 1 步：读图并拆模块

用 Read 工具逐张读取截图。按**视觉自上而下、从主到次**的顺序拆出模块，每个模块成为一个一级条目。多张截图先判断关系：同一页面的不同**状态**（未开始/进行中/已结束/空/异常）合并进同一模块分状态描述；**不同页面**则各成一份或在文档内分节。后台原型若是同一模块的列表页/弹窗/详情页/编辑页，合并进同一份 PRD 的对应模块。

### 第 2 步：读风格样例与规范（必做，按第 0.5 步选套）

- 管理后台：先读 `sample-prd-admin.md`，再读 `admin-conventions.md`。
- 客户端：先读 `sample-prd.md`，再读 `writing-rules.md`。
- 两类都追加读 `checklist.md`（产品设计注意事项总清单）。

### 第 3 步：按编号规范写作

**编号是硬约束，不得变形。客户端与后台共用全角规范，仅深层编号不同：**

客户端：

| 层级 | 符号 | 示例 |
|---|---|---|
| 一级 | `一、` `二、` … `十一、` | `一、页面标题` |
| 二级 | `1、` `2、` `3、` | `1、搜索框` |
| 三级 | `（1）` `（2）` | `（1）参赛作品信息` |
| 四级 | `①` `②` … `⑪` | `① 参赛编号：展示参赛编号` |
| 五级 | `• ` | `• 会员等级校验` |

管理后台：

| 层级 | 符号 | 示例 |
|---|---|---|
| 一级 | `一、` `二、` … | `一、查询` |
| 二级 | `1、` `2、` `3、` | `1、创建组织` |
| 三级-字段枚举 | `a.` `b.` `c.` | `a.商品编码` |
| 三级-选项/分支枚举 | `（1）` `（2）` | `（1）全部门店统一设置` |

- 全部使用**全角**序号字符，句读用中文顿号 `、` 和全角括号 `（）`。
- **禁止**使用 Markdown 的有序列表（`1.`）或标题号（`##`）承载正文编号——渲染器会自动改写序号。正文写成纯文本行，层级之间用空行分隔。
- 一级条目行只写模块名，不加冒号、不加编号说明。
- 某一级下若只有一句话说明，直接在该行下方另起一行写完，不必强行拆出下一级。

### 排版规范（字体与字号）

PRD 正文的字号与加粗规则，**所有层级共用以下基线**，与编号规范独立：

- **一级标题（一、二、三…）**：`16pt`、加粗。
- **二级及以下（1、 （1） ① • / 后台 a. b. c.）及正文**：`14pt`、不加粗。

应用方式：

- 生成 Markdown 时，一级标题行用 `**一、页面标题**` 体现加粗，并在文档顶部加一行字号说明注释（见骨架模板），方便转 docx/HTML 时套用 16pt/14pt。
- 若用户要求导出 docx/HTML，在转换层显式将一级标题设为 16pt 加粗、其余 14pt 不加粗（不依赖渲染器默认样式）。

### 第 4 步：对照 checklist 逐项自检后交付

按 `references/checklist.md` 的"一、要素检验 / 二、整体检验 / 三、列表通用需求 / 四、特殊注意事项 / 五、场景检查矩阵"逐条过一遍，凡与本功能相关的条目都要在 PRD 中有明确交代；写不出的规则就地标 `（待确认）`，不要留空。后台原型额外核对 `admin-conventions.md` 第四节列出的后台重点条目。

默认输出到工作区 `output/<页面名>-PRD.md`；用户要 Word 时再转 docx。完成后用 present_files 展示。

## 全局排除项（固定不写入 PRD）

以下属于**全局应用框架层**，不单独成模块写入 PRD：

1. **顶部导航栏**：返回按钮、页面标题文字、全局搜索 / 头像 / 更多图标等通用头部元素。
2. **底部 tab bar**：全局底部导航（如 首页 / 发现 / 我的 等通用底部栏）。

理由：这些是 App / 小程序 / H5 的通用框架，不随单个页面变化，写进单页 PRD 属于冗余，且易与全局框架规范冲突、造成重复维护。

**例外（仍需撰写）**：页面**内部**的 tab 切换，若切换的是本页自身内容（如 企业动态 / 心声、参赛作品 / 排行榜），属于页面功能，应作为内容章节撰写；但**不要**归入「顶部导航栏」条目，按其内容单独成模块（如「七、tab」）。

## 写作规范（通用）

1. **每个元素三件事齐全**：取值来源、展示规则、交互结果。
2. **符号约定**：`「」` 包字段名、按钮名、tab 名、页面名、状态名；`“”` 包给用户看的文案原文。
3. **状态优先**：凡活动/任务类页面，按三态分别说明按钮与文案显隐。
4. **极值必写**：文本超长截断、数字超阈值格式化、个位数补零。
5. **列表必写**：分页粒度、到底提示文案、排序规则（含并列次级因子）、空状态。
6. **统计口径必写**：删除后是否回退、游客是否计入、定时更新时点、是否去退货。
7. **拦截流程写优先级**：多重校验串联给出顺序。
8. **交叉引用不展开**：已有独立文档写「详见 X」。
9. **只写"做成什么样"**：不写技术实现、接口设计、验收标准。
10. **不确定项就地标注**：原型看不出按合理默认写出，行尾加 `（待确认）`。
11. **全局框架不写**：顶部导航栏、底部 tab bar 不单独成模块（见上方「全局排除项」）。

## 写作规范（管理后台专属）

1. **模块顺序固定**：查询 → 列表字段 → 创建 → 详情 → 编辑 → 删除 → 设置 → 操作日志 → 分域。
2. **列表字段逐字段**写展示内容 + 取值来源 + 格式（含流水码/拼接规则）。
3. **二次确认**：删除/转正/下架/批量操作必须写确认弹窗文案。
4. **状态机**：列全状态与流转，终态后不可再操作，延期可继续，转正/下架是否改 erp 状态写清。
5. **分域/数据权限**：总部与门店岗位可见范围、品类/门店权限边界写清。
6. **批量操作**：只操作可操作的，确认弹窗展示可操作数量。
7. **操作日志**：事件枚举 + 操作对象 + 操作人 + 时间（年月日时分秒）+ 查询条件。
8. **取数分支**：erp 提供 vs 本系统设置两种逻辑分别描述。
9. **字段校验 toast**：原文给出，变量用 `xxx` 占位、多个用分号分隔。
10. **导出**：按列表字段导出，过长数字文本化（见 checklist 77）。

## 信息缺失时的提问策略

优先自己推断。仅当缺失项会导致整块需求无法成立时才提问，一次最多 3 个关键问题。其余一律走 `（待确认）` 标注，先交出完整文档。

## 资源

- `references/sample-prd.md` — 客户端权威风格样例（真实活动页 PRD，已按本规范编号）。
- `references/sample-prd-admin.md` — 管理后台权威风格样例（真实「新品试销单」PRD，后台模块结构范本）。
- `references/writing-rules.md` — 客户端元素类型→必写规则、通用文案库、状态矩阵模板。
- `references/admin-conventions.md` — 管理后台模块结构 + 后台专属必写规则 + 后台编号规范。
- `references/checklist.md` — 产品设计注意事项总清单（要素/整体/列表/特殊/场景矩阵），交付前最终自检底线。
- `assets/prd-skeleton.md` — 客户端空白骨架。
